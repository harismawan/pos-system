# Secret Template for POS Worker
# Copy this file and replace placeholder values with base64-encoded secrets
# Usage: kubectl apply -f secret.yaml
#
# To encode secrets: echo -n "your-secret" | base64

apiVersion: v1
kind: Secret
metadata:
  name: pos-worker-secret
  namespace: pos-system
  labels:
    app: pos-worker
    app.kubernetes.io/name: pos-worker
    app.kubernetes.io/component: worker
type: Opaque
data:
  # Database URL (PostgreSQL connection string)
  DATABASE_URL: "cG9zdGdyZXNxbDovL3VzZXI6cGFzc3dvcmRAaG9zdDo1NDMyL2RhdGFiYXNl" # CHANGE_ME

  # Redis URL
  REDIS_URL: "cmVkaXM6Ly9yZWRpcy1zdmM6NjM3OQ==" # CHANGE_ME

  # SMTP Credentials
  SMTP_USER: "" # CHANGE_ME (base64 encoded)
  SMTP_PASSWORD: "" # CHANGE_ME (base64 encoded)
