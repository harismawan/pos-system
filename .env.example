# POS System Environment Variables
# ===================================
# This monorepo has separate environment files for each component.
#
# To set up your environment:
# 1. Copy backend/.env.example to backend/.env
# 2. Copy worker/.env.example to worker/.env  
# 3. Copy frontend/.env.example to frontend/.env
#
# Then update the values in each .env file according to your setup.
#
# See component-specific .env.example files for details:
# - backend/.env.example  (Database, Redis, JWT, Server, Observability config)
# - worker/.env.example   (Database, Redis, Email/SMTP, Observability config)
# - frontend/.env.example (API URL)
